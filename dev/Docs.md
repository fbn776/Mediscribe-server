# Need to know stuffs

## User verification

If the user is not verified (`profile_verified` would be false), the middleware would return a `460` status
code with a message indicating that the account is not verified.
